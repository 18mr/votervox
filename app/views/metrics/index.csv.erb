<%= CSV.generate_line ['Language Count:', @metrics[:language_count]] %>
<%= CSV.generate_line ['Site Visitors:', @metrics[:site_visitors]] %>
<%= CSV.generate_line ['Views per Language:', @metrics[:views_per_language]] %>
<%= CSV.generate_line [] %>
<%- row = ['METRIC', 'TOTAL'] + @languages.map(&:upcase) -%>
<%= CSV.generate_line row %>
<%- row = ['Total Registrations', @metrics[:registrations]] + @languages.map{ |l| @metrics[:registrations_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Total Volunteers', @metrics[:volunteer_count]] + @languages.map{ |l| @metrics[:volunteer_count_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Active Volunteers', @metrics[:active_volunteer_count]] + @languages.map{ |l| @metrics[:active_volunteer_count_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['New Volunteers', @metrics[:new_volunteer_count]] + @languages.map{ |l| @metrics[:new_volunteer_count_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Average Time per Voter', @metrics[:average_duration]] + @languages.map{ |l| @metrics[:average_duration_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Minimum Time per Voter', @metrics[:min_duration]] + @languages.map{ |l| @metrics[:min_duration_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Maximum Time per Voter', @metrics[:max_duration]] + @languages.map{ |l| @metrics[:max_duration_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Total Voters', @metrics[:voter_count]] + @languages.map{ |l| @metrics[:voter_count_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Active Matches', @metrics[:active_match_count]] + @languages.map{ |l| @metrics[:active_match_count_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['New Voters', @metrics[:new_voter_count]] + @languages.map{ |l| @metrics[:new_voter_count_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Completed Matches', @metrics[:completed_match_count]] + @languages.map{ |l| @metrics[:completed_match_count_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>
<%- row = ['Unmatched Voters', @metrics[:unmatched_voter_count]] + @languages.map{ |l| @metrics[:unmatched_voter_count_by_language][l] || 0 } -%>
<%= CSV.generate_line row %>

